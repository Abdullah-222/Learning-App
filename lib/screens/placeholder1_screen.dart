import 'package:flutter/material.dart';

class Placeholder1Screen extends StatelessWidget {
  const Placeholder1Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Coming Soon'),
    );
  }
}
