import 'package:flutter/material.dart';

class Placeholder2Screen extends StatelessWidget {
  const Placeholder2Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Coming Soon'),
    );
  }
}
